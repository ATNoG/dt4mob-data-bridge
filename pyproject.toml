[project]
name = "weather-sensor"
version = "0.1.0"
requires-python = ">=3.13.5"
dependencies = [
  "pydantic>=2.10.6",
  "pydantic-settings>=2.8.1",
  "aiohttp",
  "loguru"
]

[dependency-groups]
dev = [
    "pytest>=8.4.1",
    "ruff>=0.12.5",
    "ty>=0.0.1a16",
]
